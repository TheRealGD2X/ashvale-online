# build output and test artefacts
dist/
f_*.png
__pycache__/
*.pyc
.DS_Store
Thumbs.db
node_modules/
# files the Claude desktop app drops into the folder when a worker sends an attachment
Claude outputs/
# art pipeline scratch (renders are committed under assets/, sources are not)
art/_render/
art/_cache/
tests/_shots/

# the 3D game: engine download and the kits unpacked by tools/setup_godot.ps1
tools/godot/
# third-party model kits used by the web art pipeline (KayKit, CC0): download again from kaylousberg.itch.io if needed
art/models/hexpack/
*.blend1
